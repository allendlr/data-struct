
#pragma once
using namespace std;
class MovieList{
    private:
        struct node{
            int num, year;
            string title, genre;
            node *next;
        };
        node *head;
    public:
        MovieList();
        while(run){
            
        }
};